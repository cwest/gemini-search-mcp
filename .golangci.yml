# golangci-lint configuration (schema v2).
# Pins the default linter set explicitly so the CI gate is reproducible
# across golangci-lint releases rather than tracking shifting defaults.
version: "2"
linters:
  default: standard
  # standard = errcheck, govet, ineffassign, staticcheck, unused.
